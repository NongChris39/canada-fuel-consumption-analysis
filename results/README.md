# Results

The pipeline generates figures in `results/figures/` and CSV summaries in `results/tables/`.

Generated outputs are ignored by default. Copy selected final figures to a versioned presentation folder if you want to display them permanently on GitHub.
